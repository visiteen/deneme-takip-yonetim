import{useMemo,useState}from'react';import type{PriceHistoryRow,PriceIntelligence}from'./historyTypes';const money=(v:number)=>new Intl.NumberFormat('tr-TR',{style:'currency',currency:'TRY'}).format(v);const label=(x:string)=>x==='liste_satis'?'Liste Satış':x==='gerceklesen_satis'?'Gerçekleşen Satış':'Satınalma';export function PriceHistoryPage({rows,intelligence}:{rows:PriceHistoryRow[];intelligence:PriceIntelligence[]}){const[q,setQ]=useState(''),[type,setType]=useState('all');const filtered=useMemo(()=>rows.filter(r=>(type==='all'||r.price_type===type)&&(!q||`${r.publisher} ${r.exam_name} ${r.institution_name??''} ${r.supplier_name??''}`.toLowerCase().includes(q.toLowerCase()))),[rows,q,type]);const changes=intelligence.filter(x=>x.price_change_pct!=null&&x.price_change_pct!==0).slice(0,8);return <section><div className="page-head"><div><h2>Fiyat Geçmişi</h2><p>Liste, kurum satış ve yayınevi satınalma fiyatlarının zaman içindeki gerçek ticari izini korur.</p></div><span className="record-count">{filtered.length} kayıt</span></div><div className="price-history-grid"><div className="card price-change-panel"><h3>Son Fiyat Değişimleri</h3>{changes.map(x=><div className="price-change" key={x.id}><div><b>{x.publisher} · {x.exam_name}</b><span>{x.institution_name||x.supplier_name||label(x.price_type)}</span></div><div><strong>{money(x.unit_price)}</strong><small>{(x.price_change_pct??0)>0?'+':''}{x.price_change_pct}%</small></div></div>)}</div><div className="card price-rules"><h3>Ticari Kayıt Kuralı</h3><p><b>Liste fiyatı</b> güncellenebilir; eski sipariş değişmez.</p><p><b>Satış fiyatı</b> kurumun sipariş anındaki gerçek fiyatıdır.</p><p><b>Satınalma fiyatı</b> ilgili alımın gerçek yayınevi maliyetidir.</p><p>Yeni fiyat eski kaydı silmez.</p></div></div><div className="history-filters card"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Deneme, yayınevi, kurum veya tedarikçi ara"/><select value={type} onChange={e=>setType(e.target.value)}><option value="all">Tüm Fiyatlar</option><option value="liste_satis">Liste Satış</option><option value="gerceklesen_satis">Gerçekleşen Satış</option><option value="satinalma">Satınalma</option></select></div><div className="card pricing-table"><table><thead><tr><th>Tarih</th><th>Tür</th><th>Deneme</th><th>Kurum / Tedarikçi</th><th>Referans</th><th>Birim Fiyat</th></tr></thead><tbody>{filtered.map(r=><tr key={r.id}><td>{new Date(r.effective_date+'T12:00:00').toLocaleDateString('tr-TR')}</td><td><span className={`price-type ${r.price_type}`}>{label(r.price_type)}</span></td><td><b>{r.publisher}</b><span>{r.exam_name}</span></td><td>{r.institution_name||r.supplier_name||'Genel Liste'}</td><td>{r.order_no||r.procurement_no||'—'}</td><td><strong>{money(r.unit_price)}</strong></td></tr>)}</tbody></table></div></section>}