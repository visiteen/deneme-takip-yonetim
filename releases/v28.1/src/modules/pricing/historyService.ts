import{supabase}from'../../lib/supabase';import type{PriceHistoryRow,PriceIntelligence}from'./historyTypes';const n=(x:any)=>Number(x??0);export async function listPriceHistory(){const{data,error}=await supabase.from('price_history_detail').select('*').order('effective_date',{ascending:false});if(error)throw error;return(data??[]).map((x:any)=>({...x,unit_price:n(x.unit_price)}))as PriceHistoryRow[]}export async function listPriceIntelligence(){const{data,error}=await supabase.from('trade_price_intelligence').select('*').order('effective_date',{ascending:false});if(error)throw error;return(data??[]).map((x:any)=>({...x,unit_price:n(x.unit_price),previous_price:x.previous_price==null?null:n(x.previous_price),price_change_pct:x.price_change_pct==null?null:n(x.price_change_pct)}))as PriceIntelligence[]}